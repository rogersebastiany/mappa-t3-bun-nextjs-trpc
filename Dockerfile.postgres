# Use the official Postgres image from Docker Hub
FROM postgres:16

# Set environment variables for the default database, user, and password
ENV POSTGRES_DB=test01
ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=password

# Expose the default Postgres port
EXPOSE 5432

# Optionally, you can add initialization scripts by copying them into /docker-entrypoint-initdb.d/
# COPY ./init.sql /docker-entrypoint-initdb.d/

# No custom entrypoint needed; the default will start the server
